<UserControl
    x:Class="sparkiy.Views.Devices.IoT.Windows.DeviceSetupAutoFindDeviceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:sparkiy.Views.Devices.IoT.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!-- Header -->
		<TextBlock Text="Connecting to your device..." 
				   FontSize="{ThemeResource HubHeaderThemeFontSize}"
				   FontWeight="{ThemeResource HubHeaderThemeFontWeight}"
				   Margin="{ThemeResource HubSectionHeaderThemeMargin}"
				   VerticalAlignment="Center" HorizontalAlignment="Center" 
				   Grid.Row="1" />

		<!-- Progress indicator -->
		<ProgressRing IsActive="True" 
					  Margin="0 24 0 8" 
					  Width="32" Height="32" 
					  Grid.Row="2" />
		
    </Grid>
</UserControl>
